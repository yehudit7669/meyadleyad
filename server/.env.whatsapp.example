# ============================================
# WhatsApp Distribution Module - ENV Variables
# ============================================

# Enable/Disable the module
WHATSAPP_MODULE_ENABLED=true

# Enable/Disable digest functionality
WHATSAPP_DIGEST_ENABLED=true

# Frontend URL for generating listing links
FRONTEND_URL=https://meyadleyad.com

# Optional: Override resend requires explicit reason
# (This is enforced in code for SUPER_ADMIN only)

# ============================================
# Usage Notes
# ============================================
# - Set WHATSAPP_MODULE_ENABLED=false to disable the entire module
# - Set WHATSAPP_DIGEST_ENABLED=false to disable digest creation
# - FRONTEND_URL is used by Message Builder to create canonical URLs
# - All endpoints require authentication (JWT token)
# - Content managers can approve+send, super admins can override
